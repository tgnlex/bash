These are shell scripts that I am writing to learn bash.
To source and use as dependencies for larger scripts simply 
comment out the invocation at the end of the file.

Files in /sh/lib are dependencies intended to be used
by other scripts exclusively. 
